{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "chmod +x ./build.sh && ./build.sh"
  },
  "deploy": {
    "startCommand": "gunicorn capstoneforever.wsgi:application --bind 0.0.0.0:$PORT"
  }
}
